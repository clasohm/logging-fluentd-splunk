<source>
  @type secure_forward
  self_hostname fluentd-secure-forwarder.splunk-logging.svc
  bind 0.0.0.0
  port 24284

  shared_key "#{ENV['SF_SHARED_KEY']}"

  secure yes
  cert_path /etc/fluent/certs/secure_forward_cert.pem
  private_key_path /etc/fluent/certs/secure_forward_key.pem
</source>
